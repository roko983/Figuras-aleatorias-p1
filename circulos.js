/*class Create_ci {
    constructor() {
        this.x = random(width)
        this.y = random(height)
        this.diametro = random(5, 40)
        this.speed = 1;
        
    }

    display() {
        var r = random(1,255)
        var g = random(1,255)
        var b = random(1,255)
        fill(r,g,b)
        ellipse(this.x, this.y, this.diametro)

    }
    move() {
        this.x += random(-this.speed, this.speed)
        this.y += random(-this.speed, this.speed)
        console.log(this.x)
        console.log(this.y)
        console.log("entro a move")
    }
}*/
